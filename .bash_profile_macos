#
# .bash_profile: Only executes at login
#

#============================================================================
# PATH settings for Linux machine "hypnotoad"
#============================================================================

# Add KPP folder to tail of $PATH
export PATH="${PATH}:~/repos/KPP3/bin"

# Add ~/bin to head of $PATH
export PATH="~/bin:${PATH}"

# Add /usr/local/sbin to head of $PATH (to avoid warnings)
export PATH="/usr/local/sbin:${PATH}"

#============================================================================
# Source the .bashrc file if it exists
#============================================================================
[[ -f ~/.bashrc ]] && . ~/.bashrc
